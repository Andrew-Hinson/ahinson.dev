# HireSite, not a project gallery

ahinson.dev converts hiring managers, not visitors browsing repos. The page leads with SiteIdentity and three Fetch WorkWriteups. stream-join-lab is a PublicLab card, not the hero: it is StreamingAppWork (CDC → Flink join → Iceberg), not StudySystem, and leading with it reads as Flink/dashboard person.

Considered: an EvidenceSite or Dual gallery that featured every public repo and internal tool. That recreates the old Active/Internal project lists (toc, this site, Slack reporting, Tableau) and fails WinGate.
